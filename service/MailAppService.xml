<service>
  <id>MailAppUpdater</id>
  <name>MailApp Update Service</name>
  <description>Heartbeat и обновление MailApp от имени системы без запроса прав администратора.</description>

  <!-- Runtime is the system PowerShell (System32) — independent of the app, so
       app updates never lock the service. The script lives next to this exe in
       C:\ProgramData\MailApp\service (also outside the app folder). -->
  <executable>powershell.exe</executable>
  <arguments>-ExecutionPolicy Bypass -NoProfile -File "%BASE%\update-service.ps1"</arguments>

  <startmode>Automatic</startmode>
  <onfailure action="restart" delay="10 sec"/>

  <log mode="roll-by-size">
    <sizeThreshold>2048</sizeThreshold>
    <keepFiles>2</keepFiles>
  </log>
</service>
