node_modules/
# package-lock.json is committed for reproducible CI builds
dist/
out/
*.log
.DS_Store
Thumbs.db

# Server runtime data / local config (never committed; live in server root)
server/config.php
server/mailapp.db
server/mailapp.db-wal
server/mailapp.db-shm
